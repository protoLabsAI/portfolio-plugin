# Copy to secrets.yaml in this directory, then fill in. portfolio_spinup_team clones
# this alongside langgraph-config.yaml into each spawned team's workspace, so every
# team boots with a working gateway key (and any delegate tokens) — no hand-editing.
#
# secrets.yaml is gitignored; never commit real keys.

OPENAI_API_KEY: ""   # the gateway / model key the team's brain authenticates with
