# Copy to secrets.yaml here, then fill in. Cloned into each spawned plugin-maker team.
OPENAI_API_KEY: ""   # the gateway / model key the team's brain authenticates with
# GitHub auth comes from the host's `gh` CLI login — no token needed here.
