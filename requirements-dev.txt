# Test-only deps. At runtime langchain-core (and the fleet / delegates / project_board
# host seams) are provided by protoAgent; the conftest stubs those seams so the suite
# runs standalone in CI. The plugin itself adds no runtime pip deps.
langchain-core>=0.2
pyyaml>=6
pytest>=8
pytest-asyncio>=0.23
