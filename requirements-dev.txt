# Test-only deps. At runtime langchain-core + fastapi (and the fleet / delegates /
# project_board host seams) are provided by protoAgent; the conftest stubs those seams so
# the suite runs standalone in CI. The plugin itself adds no runtime pip deps.
langchain-core>=0.2
pyyaml>=6
pytest>=8
pytest-asyncio>=0.23
fastapi>=0.110    # the console-view router (provided by the host at runtime)
httpx>=0.27       # FastAPI TestClient transport
filelock>=3       # registry RMW locks (host provides it; used by _file_lock)
